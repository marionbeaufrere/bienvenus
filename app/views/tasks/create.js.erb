function refreshInitializer(innerHTML) {
  const initialTasks = document.getElementById('task_<%= @task.id %>');
  initialTasks.innerHTML = '<%= j render "initializer", task: @task %>';
}
refreshInitializer();
