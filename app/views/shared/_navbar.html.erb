<div class="navbar">
  <!-- Logo -->
  <h1> <%= link_to 'Bienvenus', '#', class: "logo"%> </h1>

  <!-- Right Navigation -->
  <div class="navbar-right hidden-xs hidden-sm">

    <%# if user_signed_in? %>
      <!-- Links when logged in -->
      <%#= link_to "Mon profil", '#', class: "navbar-item navbar-link" %>
      <%#= link_to "Mes documents", '#', class: "navbar-item navbar-link btn_hp" %>

      <!-- Avatar with dropdown menu -->
      <div class="navbar-item">
        <div class="dropdown">
          <%#= cl_image_tag current_user.photo, crop: :fill, class: "avatar dropdown-toggle", id: "navbar-menu", "data-toggle" => "dropdown" %>

          <ul class="dropdown-menu dropdown-menu-right navbar-dropdown-menu">
            <li>
              <%= link_to '#' do %>
                <i class="fa fa-user"></i> <%= t(".profile", default: "Mon profil") %>
              <% end %>
            </li>
            <li>
              <%= link_to '#', method: :delete do %>
              <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Me déconnecter") %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
      <%# else %>
      <!-- Login link (when logged out) -->
    <%#end %>
  </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-dropdown-menu">
        <li><a href="#">Some mobile link</a></li>
        <li><%= link_to '#' do %>
                <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
              <% end %></li>
        <li><a href="#">Other one</a></li>
      </ul>
    </div>
  </div>
</div>
